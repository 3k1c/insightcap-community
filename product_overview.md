# InsightCAP — 產品核心功能與價值主張概覽

> **定位**：InsightCAP 不是一個簡單的知識管理系統 (知識庫)，而是一個物理級的**經驗調用系統 (Experience Recall System)**。它的核心目標是讓「你過去做過的每個項目，都讓下一個項目更容易完成」。

---

## 1. 核心驅動哲學 (Core Philosophy)
*   **從「搜尋資料」到「調用經驗」**：傳統系統幫你找到資料，InsightCAP 在你工作時，主動把正確的過去經驗帶進當前工作。
*   **本地優先 (Local-first)**：所有的 Embedding、向量搜尋、LLM 推論均優先在本機運行，確保隱私與離線可用性。
*   **三層記憶架構**：
    *   **Data (資料)**：項目專屬事實與細節。
    *   **Pattern (方法論)**：跨項目的 SOP 與框架（系統會自動識別並建議升格）。
    *   **Log (風險控制)**：過去踩過的坑、應避免的決策（對話時 AI 會主動提示風險）。

---

## 2. 殺手級功能特性 (Key Features)

### 🚀 智慧擷取與入庫 (Smart Capture)
*   **全域快捷鍵**：`Ctrl+Alt+F` 一鍵擷取選取文字；`Ctrl+Alt+G` 呼叫極簡快速輸入框。
*   **深度內容解析**：自動處理網頁正文 (Readability)、YouTube 字幕萃取、Bilibili 影片字幕獲取。
*   **非同步流水線**：擷取後背景 Processor 自動完成分段 (Chunking)、Embedding 與動態聚類。

### 🧠 增強型 RAG 引擎 (AI Assistant)
*   **分層 Context 組合**：AI 回答依「編譯知識 > 方法論 (Pattern) > 風險警告 (Log) > 參考資料」優先級組裝。
*   **精確知識引用**：支援使用 `@來源` 指定文件或 `#標籤` 鎖定範圍，帶有清晰的原始出處鏈接。
*   **上下文提示 (ContextHint)**：對話時即時顯示 AI 在本次回答中參考了哪些經驗。

### ✍️ AI 協同文本編輯器
*   **整合式 Workspace**：Chat 頁面右側內建基於 Tiptap 的排版編輯器。
*   **AI 選文優化**：選取文字即可進行改寫、語法修正、風格調整，支援 Diffs 預覽後替換。
*   **雙向入庫**：文稿匯出時自動同步至知識庫，讓筆記也能成為未來的經驗素材。

### 📱 多端連動生態
*   **Telegram Bot**：行動端隨時投餵想法，或與個人 AI 助手對話。
*   **手機端 (Phase 6)**：支援離線推理並與桌面端數據同步。

---

## 3. 視覺與交互設計 (UI/UX)
*   **極致美學**：支援 Frost (亮色)、Void (暗色)、Warm (羊皮紙)、Sage (森林) 四套高品質主題佈景。
*   **Timeline 導向儲存庫**：以時間軸組織知識來源，用戶視圖以「原文件」為單位展示。
*   **全介面多語言**：原生支援繁體中文、簡體中文與英文切換。

---

## 4. 技術與安全保障 (Tech Stack & Security)
*   **計算邊緣化**：基於 Tauri 2.0 (Rust) + React，兼顧桌面效能與 UI 靈活性。
*   **資料強加密**：本地 SQLite 經由 SQLCipher 強度加密，且搭配 Argon2id 密鑰管理。
*   **向量引擎**：使用高效能的 usearch 本地索引與 fastembed-rs 向量化工具。
